# Copia este archivo a .env y rellena las variables.
# .env NO debe commitearse al repo (añadirlo a .gitignore).

# Contraseña root de MySQL (usada también por la API para conectar).
MYSQL_ROOT_PASSWORD=cambia_esto
